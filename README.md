# The Lost Princess of Sardegna

A single self-contained static HTML page (a quest/score board game). No build step, no dependencies.

- `index.html` — the entire site.
- Deployed on Vercel; redeploys automatically on push to `main`.
